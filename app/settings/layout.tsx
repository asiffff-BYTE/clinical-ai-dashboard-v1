export const metadata = {
  title: "Settings - ClinicalAI",
  description: "Configure alert thresholds, system options, and user preferences.",
}

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
